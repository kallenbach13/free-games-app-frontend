import React from 'react';

const Search = () => {
  return (
    <div className="filter">
      <input
        id="search-bar"
        type="text"
        placeholder="Search for game title"
      />
    </div>
  );
}

export default Search;